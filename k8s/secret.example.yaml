# Not applied as-is — create with:
#   kubectl create secret generic cloudflare-exporter-token \
#     --from-literal=CLOUDFLARE_API_TOKEN=<your-token>
apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-exporter-token
type: Opaque
stringData:
  CLOUDFLARE_API_TOKEN: "changeme"
